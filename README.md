## OpenSCAD Threads library

This is a fork of https://dkprojects.net/openscad-threads/

I added an optional tolerance parameter to better compute the number of segments needed on each turn.

If the parameter is not defined, the tolernce is set to pitch / 5.
